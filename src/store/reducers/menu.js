import {
  FETCH_MENU_REQUESTED,
  FETCH_MENU_SUCCEEDED,
  FETCH_MENU_FAILED,
} from '../constants';

const cart = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default cart;
