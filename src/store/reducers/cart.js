import {
  ADD_CART_ITEM,
  REMOVE_CART_ITEM,
  FINISH_PURCHASE,
} from '../constants';

const cart = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default cart;
