import React from 'react';

const Recommendations = () => (
  <section>
    <h4>Recommendations</h4>
    <ul>
      <li>Use a code coverage tool</li>
      <li>If it is hard to test, could mean there is more complexity than it should</li>
      <li>The fewer mocks, the better</li>
    </ul>
  </section>
);

export default Recommendations;
